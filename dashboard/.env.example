VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Create a local .env file with real values (never commit secrets):
# cp .env.example .env
# Then edit .env and paste your real credentials.